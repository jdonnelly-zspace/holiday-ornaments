rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Allow uploads to submissions folder
    match /submissions/{allPaths=**} {
      allow read: if true;
      allow write: if true;
    }
  }
}
